/**
  strings for general use via emojify
 */
export type Strings = {
  infoBanner: string
}

export const strings: Strings = {
  infoBanner: `\n
          Automate all the things! :fire: \n
          :sparkles:  This CLI is aimed to automate react code generation when using some widely used libraries :sparkles: \n
          Lets start! ... :rocket: \n\n`,
}
